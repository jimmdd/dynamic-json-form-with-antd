export default ({ query: { sid } }, res) => {
    // Fetch schema from db


    // Schema with id exists
    // if (filtered.length > 0) {
    //   res.status(200).json(filtered[0])
    // } else {
    //   res.status(404).json({ message: `User with id: ${id} not found.` })
    // }
}